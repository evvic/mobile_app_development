import 'package:flutter/material.dart';
import 'package:flutter_weather/screens/current_weather.dart';

void main() {
  runApp(const MaterialApp(
    title: 'Navigation Basics',
    home: currentWeatherOnly(),
  ));
}